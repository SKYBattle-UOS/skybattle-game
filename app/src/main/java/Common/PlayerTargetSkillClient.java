package Common;

public abstract class PlayerTargetSkillClient extends PlayerTargetSkill {
    public PlayerTargetSkillClient(MatchCommon match, UIManager uiManager) {
        super(match, uiManager);
    }
}
